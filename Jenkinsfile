pipeline {
    agent any
    stages {
        stage('compile') {
            steps {
              echo 'Enter Compile'
              echo "workspace" $WORKSPACE
       		    
		          }
        }
        stage('build'){
            steps{
                echo 'Enter Build'
            
            }
        }
        stage('archive'){
            steps{
                echo 'Enter Archive'
            }
        }
    }
}
